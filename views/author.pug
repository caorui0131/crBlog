extends layout

block content
  div.main
    h1 作者主页
    div.blogList
      each blog in blogList
        div.blogItem
          a(href='/blog/detail/'+blog.id target="_blank") #{blog.title}
          div.blogItem-info
            a.author(href='/user/'+blog.author target="_blank") #{blog.author}&nbsp;
            span #{blog.createtime}
